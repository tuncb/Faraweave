# Remove documentation contract tests

CI validates executable, build, packaging, and release behavior, but does not test documentation or traceability artifacts. Documentation can therefore be changed or removed independently of CI.
