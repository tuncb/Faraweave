# Issue #97 — placeholder-free connected completion

Connected templates are a flat, stack-safe parser form that lower right-to-left into existing selected calls, with no FWIR version, feature, node, access, or dependency. Immediate authored tuples splice their elements, while other operands remain one value and runtime tuple bundles are rejected. Bracket and tuple sibling lists keep their existing separator grammar, and incomplete calls never become callable values.
