# Issue #53 — backend-native `cos`

Primitive ID 34 uses signature and implementation IDs 40 with the existing unary Double elementwise plan, Int promotion, semantic 1.1, and mandatory feature 7. Rust calls `f64::cos` and generated C calls `<math.h>` `cos` inside the strict floating environment, producing exact `1` for either zero and canonicalizing non-finite NaNs without observing `errno` or exception flags. Finite results use the backend-native eight-ULP-or-2^-48-absolute envelope, including deliberately platform-sensitive large-argument range reduction, while resources, diagnostics, ownership, cleanup, and publication remain exact.
